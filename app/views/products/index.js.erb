$('.products-listing').html('<%= j render(@products) %>');
$('.products-count').html("<%= pluralize(@products.count, 'matching products') %>");
$('.pagination').replaceWith('<%= j will_paginate(@products) %>');